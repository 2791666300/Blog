name: deploy

on:
  push

jobs:
  deploy
    runs-on: ubuntu-latest
    steps:
      - 
      - name: checkout
        uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: "17"
      - name: build frontend
      - run: cd / & cd Blog

